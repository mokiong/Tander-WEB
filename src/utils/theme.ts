import { extendTheme } from "@chakra-ui/core";

export const theme = extendTheme({
  colors: {
    tinder: {
      primary: "#FF655B",
      layout: "#FE3C72",
      secondaryBg: "#424242",
      bg: "#424242",
    },
  },
  fonts: {
    tinder: {
      logo: "system-ui, sans-serif",
    },
  },
});
