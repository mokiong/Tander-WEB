subscription NewMessage($loggedUser: Int!, $receiver: Int!) {
  newMessage(loggedUser: $loggedUser, receiver: $receiver) {
    id
    text
    user {
      id
      username
    }
    receiver {
      id
      username
    }
    createdAt
  }
}
