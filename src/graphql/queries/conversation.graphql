query Conversation($receiverId: Int!) {
  conversation(receiverId: $receiverId) {
    id
    text
    user {
      username
      id
    }
    receiver {
      username
      id
    }
    createdAt
    updatedAt
  }
}
